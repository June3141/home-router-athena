---
- name: Configure IP forwarding via sysctl
  ansible.builtin.import_tasks: sysctl.yml
  tags: [network, sysctl]

- name: Configure systemd-networkd interfaces
  ansible.builtin.import_tasks: systemd-networkd.yml
  tags: [network, systemd-networkd]

- name: Configure nftables NAT and firewall
  ansible.builtin.import_tasks: nftables.yml
  tags: [network, nftables]

- name: Configure dnsmasq DHCP server
  ansible.builtin.import_tasks: dnsmasq.yml
  tags: [network, dnsmasq]
