---
# 全体エントリポイント
# 各 Phase の playbook を順に import する。

- name: Phase 1 - ネットワーク基盤
  ansible.builtin.import_playbook: network.yml

- name: Phase 2 - DNS スタック
  ansible.builtin.import_playbook: dns.yml

# Phase 3 以降は雛形のみ。実装完了次第 import を有効化する。
# - name: Phase 3 - Netbird Subnet Router
#   ansible.builtin.import_playbook: tailscale.yml
#
# - name: Phase 4 - 監視
#   ansible.builtin.import_playbook: monitoring.yml
