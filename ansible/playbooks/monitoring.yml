---
# Phase 4: Prometheus node_exporter（実装は後続）
- name: Phase 4 - 監視
  hosts: routers
  become: true
  gather_facts: true
  vars_files:
    - ../vars/common.yml
  roles:
    - role: node_exporter
      tags: [monitoring, node_exporter]
